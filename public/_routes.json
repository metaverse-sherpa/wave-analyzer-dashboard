{
  "version": 1,
  "include": ["/*"],
  "exclude": [
    "/assets/*",
    "/build/*"
  ],
  "routes": [
    {
      "pattern": "/api/*",
      "handler": "worker"
    },
    {
      "pattern": "/*",
      "handler": "static"
    }
  ]
}